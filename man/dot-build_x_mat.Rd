% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcsimex-internal.R
\name{.build_x_mat}
\alias{.build_x_mat}
\title{Build x_mat from a clean formula, preserving I(), interactions, offsets, etc.}
\usage{
.build_x_mat(clean_formula, mc_variables, data, xlev = NULL)
}
\arguments{
\item{clean_formula}{the formula with mc() wrappers stripped (bare variable names remain)}

\item{mc_variables}{character vector of mc variable names to exclude}

\item{data}{the data frame}

\item{xlev}{a named list of factor levels to enforce (as in
\code{\link[stats]{model.frame}}); used by \code{predict.simex} so newdata
is coded with the training levels}
}
\value{
a list with x_mat (the non-mc part of the model matrix) and
  x_terms (the terms object, for use in predict)
}
\description{
Replaces the fragile all.vars() + reformulate() approach. Uses model.matrix()
on the clean formula and removes columns belonging to mc variables.
}
\keyword{internal}
