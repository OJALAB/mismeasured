% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcglm-sandwich.R
\name{mc_parse_formula}
\alias{mc_parse_formula}
\title{Parse an \code{mc()} model formula}
\usage{
mc_parse_formula(
  formula,
  data,
  env = parent.frame(),
  require_y = TRUE,
  z_levels = NULL,
  x_levels = NULL
)
}
\arguments{
\item{formula}{A formula with exactly one \code{\link{mc}} term on the
right-hand side, e.g. \code{y ~ mc(z, Pi) + x1}.}

\item{data}{A data frame containing the variables.}

\item{env}{Environment in which to evaluate the \code{mc()} matrix
argument (default: the caller's environment).}

\item{require_y}{Logical. If \code{FALSE}, a missing response column in
\code{data} is allowed and \code{y} is returned as \code{NULL} --
useful when parsing prediction data (e.g. a probability sample that
carries no outcome).}

\item{z_levels}{Optional character vector of reference levels for the
\code{mc()} variable (e.g. \code{fit$z_levels} from a fitted
\code{mcglm}). When supplied, a factor/character \code{mc()} variable
is recoded \emph{against these levels} -- the safe way to parse new
data, whose own factor level set or order may differ -- and unseen
levels raise an error. Ignored for integer-coded variables.}

\item{x_levels}{Optional named list of reference factor levels for the
remaining covariates (e.g. \code{fit$x_levels}), applied via
\code{model.frame(xlev = )} exactly like \code{predict.lm}.}
}
\value{
A list with components \code{y} (numeric response, or
  \code{NULL} when absent and \code{require_y = FALSE}),
  \code{z_hat} (integer proxy, coded \code{0, ..., K-1}),
  \code{z_levels}/\code{x_levels} (factor bookkeeping for later
  prediction; \code{NULL}/empty for integer inputs),
  \code{x} (model matrix of the remaining terms, including an
  intercept), \code{Pi} (the \eqn{K \times K} misclassification
  matrix, or \code{NULL} if not supplied inside \code{mc()}), and
  \code{K} (number of categories).
}
\description{
Public interface to the formula parsing used by \code{\link{mcglm}}:
splits \code{y ~ mc(z, Pi) + x1 + ...} into the response, the 0-based
proxy covariate, the model matrix of the remaining terms, and the
misclassification matrix. Intended for packages building on
\pkg{mismeasured} (e.g. survey-integration wrappers) that must
intercept the \code{mc()} term before standard formula machinery
(such as \code{\link[stats]{model.matrix}}) sees it.
}
\examples{
Pi <- matrix(c(0.9, 0.1, 0.15, 0.85), 2, 2)
df <- data.frame(y = rpois(50, 1), z = rbinom(50, 1, 0.4),
                 x1 = rnorm(50))
parts <- mc_parse_formula(y ~ mc(z, Pi) + x1, df)
str(parts)

}
\seealso{
\code{\link{mc}}, \code{\link{mcglm}}
}
