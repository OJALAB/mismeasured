% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misclass.R
\name{build.mc.matrix}
\alias{build.mc.matrix}
\title{Build a valid misclassification matrix}
\usage{
build.mc.matrix(
  mc.matrix,
  method = "series",
  tuning = sqrt(.Machine$double.eps),
  diag.cor = FALSE,
  tol = .Machine$double.eps,
  max.iter = 100
)
}
\arguments{
\item{mc.matrix}{an empirical misclassification matrix.}

\item{method}{method for estimating the generator: \code{"series"} (default),
\code{"log"}, or \code{"jlt"} (Jarrow-Lando-Turnbull).}

\item{tuning}{small positive constant for numerical stability (default:
\code{sqrt(.Machine$double.eps)}).}

\item{diag.cor}{logical; if \code{TRUE}, corrections are subtracted from the
diagonal only. If \code{FALSE} (default), distributed proportionally.}

\item{tol}{convergence tolerance for the series method.}

\item{max.iter}{maximum iterations for the series method.}
}
\value{
A valid misclassification matrix.
}
\description{
Estimates the nearest misclassification matrix that can be raised to
fractional powers (i.e., has a valid matrix logarithm/generator).
}
\examples{
Pi <- matrix(c(0.989, 0.01, 0.001, 0.17, 0.829, 0.001, 0.001, 0.18, 0.819),
             nrow = 3, byrow = FALSE)
check.mc.matrix(list(Pi))
Pi_fixed <- build.mc.matrix(Pi)
check.mc.matrix(list(Pi_fixed))

}
\references{
Israel, R.B., Rosenthal, J.S., Wei, J.Z. (2001). Finding generators for
Markov Chains via empirical transition matrices, with applications to credit
ratings. \emph{Mathematical Finance}, 11, 245--265.
}
