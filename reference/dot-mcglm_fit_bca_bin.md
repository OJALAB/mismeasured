# Additive bias correction (BCA) for binary misclassification

Additive bias correction (BCA) for binary misclassification

## Usage

``` r
.mcglm_fit_bca_bin(
  psi_naive,
  y,
  xi_hat,
  x,
  family,
  c1,
  c2,
  iterate = FALSE,
  max_iter = 50,
  tol = 1e-08,
  wt = NULL
)
```
