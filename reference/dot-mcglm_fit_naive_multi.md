# Fit the naive GLM estimator (multicategory misclassification)

Fit the naive GLM estimator (multicategory misclassification)

## Usage

``` r
.mcglm_fit_naive_multi(y, xi_hat, family, wt = NULL)
```
