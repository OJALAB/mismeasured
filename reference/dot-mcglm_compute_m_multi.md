# Compute per-observation drift m_i(psi) for multicategory misclassification

Compute per-observation drift m_i(psi) for multicategory
misclassification

## Usage

``` r
.mcglm_compute_m_multi(psi, x, K, mu_fun, Pi, pi_z)
```
