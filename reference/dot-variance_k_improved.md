# Variance estimation for fixed-Pi K-level improved MC-SIMEX

Variance estimation for fixed-Pi K-level improved MC-SIMEX

## Usage

``` r
.variance_k_improved(theta_list, naive_refit, transform, lambda, B, p)
```
