# Linear transformation matrix for fixed-Pi improved MC-SIMEX

Linear transformation matrix for fixed-Pi improved MC-SIMEX

## Usage

``` r
.k_improved_transform(p, C, alpha, intercept_idx = integer(0))
```
