# Compute per-observation drift m_hat(psi) for binary misclassification

Compute per-observation drift m_hat(psi) for binary misclassification

## Usage

``` r
.mcglm_compute_m_bin(psi, x, mu_fun, c1, c2)
```
