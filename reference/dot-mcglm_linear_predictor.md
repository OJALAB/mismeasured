# Linear predictor at arbitrary coefficients (binary or multicategory: the dummy encoding in xi_hat picks out the z-specific gamma, so a single matrix product covers both cases)

Linear predictor at arbitrary coefficients (binary or multicategory: the
dummy encoding in xi_hat picks out the z-specific gamma, so a single
matrix product covers both cases)

## Usage

``` r
.mcglm_linear_predictor(object, psi)
```
