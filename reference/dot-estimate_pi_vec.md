# Estimate latent category probabilities from observed categories and Pi

Estimate latent category probabilities from observed categories and Pi

## Usage

``` r
.estimate_pi_vec(z_hat, Pi, wt = NULL, eps = 1e-06)
```
