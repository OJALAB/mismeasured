# Formula of an `mcglm` fit (NULL for matrix interface)

Formula of an `mcglm` fit (NULL for matrix interface)

## Usage

``` r
# S3 method for class 'mcglm'
formula(x, ...)
```

## Arguments

- x:

  An `mcglm` object.

- ...:

  Unused.
