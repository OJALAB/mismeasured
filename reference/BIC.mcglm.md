# BIC of an `mcglm` fit

BIC of an `mcglm` fit

## Usage

``` r
# S3 method for class 'mcglm'
BIC(object, ...)
```

## Arguments

- object:

  An `mcglm` object.

- ...:

  Additional arguments (ignored).
