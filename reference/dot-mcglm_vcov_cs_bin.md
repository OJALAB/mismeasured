# Sandwich variance for corrected-score estimator (binary)

Sandwich variance for corrected-score estimator (binary)

## Usage

``` r
.mcglm_vcov_cs_bin(
  psi,
  y,
  xi_hat,
  x,
  family,
  p01,
  p10,
  pi_z,
  c1 = NULL,
  c2 = NULL,
  wt = NULL
)
```
