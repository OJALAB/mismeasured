# Sandwich variance for corrected-score estimator (multicategory)

Sandwich variance for corrected-score estimator (multicategory)

## Usage

``` r
.mcglm_vcov_cs_multi(
  psi,
  y,
  xi_hat,
  z_hat,
  x,
  K,
  family,
  Pi,
  pi_z,
  wt = NULL,
  jacobian = c("analytical", "numerical")
)
```
