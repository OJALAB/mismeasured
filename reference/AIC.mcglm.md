# AIC of an `mcglm` fit

AIC of an `mcglm` fit

## Usage

``` r
# S3 method for class 'mcglm'
AIC(object, ..., k = 2)
```

## Arguments

- object:

  An `mcglm` object.

- ...:

  Additional arguments (ignored).

- k:

  Penalty per parameter (default 2).
