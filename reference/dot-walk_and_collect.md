# Walk a formula node, collect me/mc descriptors, return cleaned node

Walk a formula node, collect me/mc descriptors, return cleaned node

## Usage

``` r
.walk_and_collect(node, data, env, descriptors, depth, inside_interaction)
```
